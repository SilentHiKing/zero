<resources>

    <!-- Base application  theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="RecyclerTabLayoutTheme">
        <item name="android:textColorSecondary">@color/primary_light_alpha50</item>
    </style>

    <style name="Popupwindow" parent="android:style/Animation">
        <item name="android:windowEnterAnimation">@anim/set_enter_default</item>
        <item name="android:windowExitAnimation">@anim/set_exit_default</item>
    </style>


</resources>
